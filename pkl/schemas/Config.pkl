module Config

databaseUrl: String
payloadSecret: String
blobReadWriteToken: String?

output {
  text = """
    DATABASE_URL=\(databaseUrl)
    PAYLOAD_SECRET=\(payloadSecret)
    \(if (blobReadWriteToken != null) "BLOB_READ_WRITE_TOKEN=\(blobReadWriteToken)" else "")
    """
}
